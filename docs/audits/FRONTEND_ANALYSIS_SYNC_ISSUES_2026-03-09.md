# Frontend Analysis Sync Issues Register

Date: 2026-03-09  
Legend:
- Severity: Critical / High / Medium / Low
- Status: Open / Resolved

| ID | Severity | Status | Issue | Evidence | Impact | Recommended Fix |
|---|---|---|---|---|---|---|
| FA-001 | Critical | Resolved | Re-opening an already completed session can deadlock on loading screen. Backend sends `terminal_state` without result/progress, hook marks connection `finished`, store remains empty, page guard keeps showing `LoadingState`. | `apps/api/src/routes/stream.ts:124`, `apps/api/src/routes/stream.ts:145`, `apps/web/lib/use-stream-progress.ts:135`, `apps/web/app/rectify/[id]/page.tsx:391` | User cannot review already-completed analysis on analysis page. | On terminal `complete`, backend should emit result payload (`rectifiedTime`, `accuracy`, `confidence`) and metadata snapshot; frontend should map terminal complete to store completion state. |
| FA-002 | Critical | Resolved | Polling-only completion path does not provide `StreamResult`; hook dispatches `complete` with progress payload and page requires `isComplete && result` for completion CTA. | `apps/web/lib/use-stream-progress.ts:240`, `apps/web/lib/use-stream-progress.ts:243`, `apps/api/src/routes/progress.ts:96`, `apps/web/app/rectify/[id]/page.tsx:516` | In polling fallback, analysis may finish but “View Official Report” completion block never appears. | Return terminal result fields in `/api/queue/progress` for `status=complete`, or fetch final session result before dispatching complete. |
| FA-003 | High | Resolved | Zustand state is mutated directly (`lastEventId`) instead of via `set`, bypassing reactivity and middleware guarantees. | `apps/web/lib/use-stream-progress.ts:413` | Non-deterministic state updates and hard-to-debug sync issues on reconnect logic. | Add explicit store action `setLastEventId(seq)` and call through Zustand `set`. |
| FA-004 | High | Resolved | Error schema mismatch across stack (`error` on backend vs `message` expected on frontend) yields generic/garbled error text (`Unknown error` or `[object Object]`). | `apps/api/src/routes/stream.ts:60`, `apps/web/lib/use-stream-progress.ts:133`, `apps/web/lib/store/stream-store.ts:496` | Poor user diagnostics and weaker observability during failures/auth issues. | Standardize `{ type:'error', message, code }` across backend; in store fallback to `payload.error || payload.message`. |
| FA-005 | High | Resolved | Sensitive token exposure risk in frontend logging and URL composition (`sid`, `hf_token`, token prefixes, full response logging). | `apps/web/lib/use-stream-progress.ts:355`, `apps/web/lib/use-stream-progress.ts:367`, `apps/web/lib/api-client.ts:30`, `apps/web/lib/api-client.ts:67` | Token leakage via browser logs/devtools/remote telemetry. | Remove raw token URL logging, avoid query-token auth where possible, redact all auth-bearing fields before logging. |
| FA-006 | High | Resolved | Double auth middleware execution on hot routes (`/queue/progress`, `/stream`, `/queue`) due both global mount and route-level auth. | `apps/api/src/routes/index.ts:96`, `apps/api/src/routes/index.ts:107`, `apps/api/src/routes/progress.ts:17`, `apps/api/src/routes/stream.ts:49`, `apps/api/src/routes/queue.ts:29` | Extra latency and token verification load on high-frequency polling/SSE setup. | Keep auth in one layer only (prefer top-level mount) and remove duplicates in child routers. |
| FA-007 | High | Resolved | Session ID extraction conflict in stream route (`sid` used as session id fallback while also used as auth token query param). | `apps/api/src/routes/stream.ts:50`, `apps/api/src/routes/stream.ts:52` | Query-param stream variant can misread token as session ID and return false NOT_FOUND/authorization behavior. | Use `req.query.sessionId` for session id, reserve `sid` exclusively for auth token. |
| FA-008 | Medium | Resolved | Metadata event with `status=pending|queued` hard-resets full store; out-of-order metadata can wipe live progress/candidates mid-session. | `apps/web/lib/store/stream-store.ts:510`, `apps/web/lib/store/stream-store.ts:512` | Visual regressions/flicker and apparent “analysis restarted” behavior during race conditions. | Gate resets by session lifecycle transition (new session only), not by standalone metadata packet. |
| FA-009 | Medium | Resolved | Polling token cache can get stale: cached token reused indefinitely on HTTP 401; proactive refresh does not update `cachedTokenRef`. | `apps/web/lib/use-stream-progress.ts:164`, `apps/web/lib/use-stream-progress.ts:216`, `apps/web/lib/use-stream-progress.ts:555` | Long-running sessions can silently degrade into repeated poll failures/backoff loops. | On 401 clear cached token and re-acquire with `skipCache`; store refreshed token into `cachedTokenRef`. |
| FA-010 | Medium | Resolved | Retry action on error view pushes same route path; app-router may no-op and not force reconnection. | `apps/web/app/rectify/[id]/page.tsx:396` | “Retry” can appear broken to user on disconnected states. | Use explicit reconnect action or `router.refresh()`/hard reload fallback. |
| FA-011 | Medium | Resolved | Candidate rendering assumes `moon` string exists when `minifiedEph` exists (`split` called without null-safe chain). | `apps/web/components/rectify/analysis/StageLeaderboard.tsx:229` | Runtime crash risk with partial score payloads. | Use safe access (`s.minifiedEph?.moon?.split(' ')[0] ?? '-'`). |
| FA-012 | Medium | Resolved | Cancel flow updates DB status to failed but does not emit terminal stream event; live SSE consumers may not get immediate terminal signal. | `apps/api/src/lib/queue-manager.ts:431`, `apps/api/src/lib/queue-manager.ts:448`, `apps/api/src/routes/stream.ts:353` | External viewers/tabs can remain in stale “live” state until reconnect/poll/manual refresh. | Emit explicit cancel/error event through `sessionEvents` when cancel is committed. |
| FA-013 | Low | Resolved | SSE connection observability counter can overcount: increment happens before ownership/session checks; early returns do not decrement. | `apps/api/src/routes/stream.ts:72`, `apps/api/src/routes/stream.ts:93`, `apps/api/src/routes/stream.ts:370` | Misleading active connection metrics and false alarms in monitoring. | Increment only after successful stream setup or decrement on all early exits. |
| FA-014 | Low | Resolved | Excess `console.*` in critical runtime paths (`use-stream-progress`, store, API client, reasoning UI). | `apps/web/lib/use-stream-progress.ts:384`, `apps/web/lib/store/stream-store.ts:170`, `apps/web/lib/api-client.ts:18`, `apps/web/components/rectify/UnifiedAIPanel/index.tsx:37` | Noisy logs, potential accidental data exposure, hard production debugging signal-to-noise. | Replace with sanitized structured logger and environment-gated debug flags. |
| FA-015 | Low | Resolved | Test blind spots: no explicit tests for `terminal_state` complete hydration, polling-only completion result handoff, or standardized error payload contract. | `apps/web/lib/__tests__/use-stream-progress.test.ts:303`, `apps/web/lib/store/__tests__/stream-store.test.ts:489`, `apps/api/src/routes/__tests__/stream.test.ts:194` | Regressions in critical edge flows can ship undetected despite green suites. | Add contract tests covering terminal complete replay, polling complete with result, and `error.message` schema assertions. |

## Verification (2026-03-09)
- `npm run -w @ai-pandit/web test -- lib/__tests__/use-stream-progress.test.ts lib/store/__tests__/stream-store.test.ts --run` (pass)
- `npm run -w @ai-pandit/api test -- src/routes/__tests__/stream.test.ts src/routes/__tests__/progress.test.ts src/lib/__tests__/queue-manager.test.ts --run` (pass)
- `npm run -w @ai-pandit/api typecheck` (pass)
